# # .github/workflows/deploy.yml
# name: Deploy Saboteur Game

# on:
#   push:
#     branches:
#       - main

# permissions:
#   contents: write
#   pages: write
#   id-token: write

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           # Встроенное быстрое кэширование npm пакетов
#           cache: 'npm'

#       # Дополнительное глубокое кэширование папки node_modules и кэша Vite
#       - name: Cache dependencies and bundler cache
#         uses: actions/cache@v4
#         with:
#           path: |
#             node_modules
#             ~/.npm
#             dist
#           key: ${{ runner.os }}-build-cache-${{ hashFiles('**/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.os }}-build-cache-

#       - name: Install dependencies
#         run: npm ci

#       - name: Build project
#         run: npm run build

#       - name: Deploy to GitHub Pages
#         uses: JamesIves/github-pages-deploy-action@v4
#         with:
#           folder: dist
#           branch: gh-pages